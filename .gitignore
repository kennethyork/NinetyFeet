# Godot's own build and import cache
.godot/
.import/

# Exported builds and the packaged downloads. Hundreds of megabytes of binaries
# that are reproducible from the source beside them, which is not what a
# repository is for.
build/
dist/

# .NET intermediates
bin/
obj/
